#pragma once

#include "lib/GameData.hpp"

namespace game {
    namespace physics {
        void update_players_cam(GameData &gameData, [[maybe_unused]] DebugConfiguration &debugConfiguration);
    }
}
